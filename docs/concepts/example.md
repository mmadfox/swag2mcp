# Example

See swag2mcp in action — from API discovery to real-time invocation, all through natural language.

<video src="/example.mp4" autoplay loop muted playsinline style="width:100%;max-width:800px;border-radius:8px"></video>

This walkthrough shows how an LLM agent uses swag2mcp's MCP tools to browse API specifications, inspect endpoints, and execute live API calls — without writing a single line of integration code.

import{_ as a,o as t,c as s,a2 as i}from"./chunks/framework.CHeM0PsO.js";const u=JSON.parse('{"title":"update","description":"","frontmatter":{},"headers":[],"relativePath":"cli/update.md","filePath":"cli/update.md","lastUpdated":1784904021000}'),o={name:"cli/update.md"};function n(l,e,r,d,c,h){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="update" tabindex="-1">update <a class="header-anchor" href="#update" aria-label="Permalink to &quot;update&quot;">​</a></h1><h2 id="purpose" tabindex="-1">Purpose <a class="header-anchor" href="#purpose" aria-label="Permalink to &quot;Purpose&quot;">​</a></h2><p>Re-validate the configuration, clear the cache, and re-download all spec files. This is a <strong>full refresh</strong> of the workspace — it ensures all cached specs are up to date and the index is rebuilt.</p><h2 id="when-to-use" tabindex="-1">When to use <a class="header-anchor" href="#when-to-use" aria-label="Permalink to &quot;When to use&quot;">​</a></h2><ul><li>Remote spec files have changed and you want the latest version</li><li>After editing <code>swag2mcp.yaml</code> to add or change spec locations</li><li>When troubleshooting stale or corrupted cache</li><li>Before running <code>mcp</code> to ensure everything is fresh</li></ul><h2 id="syntax" tabindex="-1">Syntax <a class="header-anchor" href="#syntax" aria-label="Permalink to &quot;Syntax&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">swag2mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [path]</span></span></code></pre></div><h2 id="arguments" tabindex="-1">Arguments <a class="header-anchor" href="#arguments" aria-label="Permalink to &quot;Arguments&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Argument</th><th>Position</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>path</code></td><td>1</td><td>No</td><td>Workspace directory. If omitted, resolves via path resolution rules.</td></tr></tbody></table><h2 id="flags" tabindex="-1">Flags <a class="header-anchor" href="#flags" aria-label="Permalink to &quot;Flags&quot;">​</a></h2><p>None.</p><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h2><p>The <code>update</code> command runs a pipeline of operations:</p><ol><li><strong>Load config</strong> — reads <code>swag2mcp.yaml</code> from the workspace</li><li><strong>Validate</strong> — runs the same checks as <code>validate</code> (YAML syntax, structure, spec file reachability, format, auth, HTTP client)</li><li><strong>Clean</strong> — removes all contents of <code>cache/</code> and <code>responses/</code></li><li><strong>Re-cache</strong> — downloads all remote spec files and copies local spec files into the cache</li><li><strong>Re-index</strong> — rebuilds the full-text search index for all endpoints</li><li><strong>Auth scripts</strong> — creates stub auth scripts for specs using <code>ScriptAuth</code></li><li><strong>Orphan cleanup</strong> — removes auth scripts for specs that no longer exist</li></ol><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">swag2mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">swag2mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> update</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./my-workspace</span></span></code></pre></div><h2 id="what-happens-to-disabled-collections" tabindex="-1">What happens to disabled collections <a class="header-anchor" href="#what-happens-to-disabled-collections" aria-label="Permalink to &quot;What happens to disabled collections&quot;">​</a></h2><p>Collections with <code>disable: true</code> are skipped entirely — they are not cached or indexed.</p><h2 id="post-command-verification" tabindex="-1">Post-command verification <a class="header-anchor" href="#post-command-verification" aria-label="Permalink to &quot;Post-command verification&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">swag2mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ls</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [path]</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># All specs should still be listed and reachable</span></span></code></pre></div><h2 id="nuances" tabindex="-1">Nuances <a class="header-anchor" href="#nuances" aria-label="Permalink to &quot;Nuances&quot;">​</a></h2><ul><li><strong>No auto-init:</strong> If the config file does not exist, <code>update</code> returns an error: <code>&quot;configuration not found at &amp;lt;path&amp;gt;&quot;</code>. Run <code>init</code> first.</li><li><strong>Network dependency:</strong> All remote spec URLs must be reachable. If any download fails, the entire update fails with a clear error message.</li><li><strong>Auth script creation:</strong> If a spec uses <code>ScriptAuth</code> and the stub script doesn&#39;t exist, <code>update</code> creates it. If creation fails, the update fails.</li><li><strong><code>update</code> vs <code>clean</code>:</strong> <code>clean</code> only removes cache. <code>update</code> removes cache <strong>and</strong> re-downloads everything. Use <code>clean</code> when you just want to free space; use <code>update</code> when you want to refresh.</li></ul>`,21)])])}const g=a(o,[["render",n]]);export{u as __pageData,g as default};
